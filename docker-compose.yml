services:
  test:
    image: python:3.11.11-slim
    volumes:
      - .:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    working_dir: /workspace
    environment:
      - PYTHONPATH=/workspace
    command: bash -c "pip install -r requirements.txt && pytest -v"
